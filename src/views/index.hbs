<main>
    <h1>Browser</h1>
    <form action="" method="">
        <input type="text" class="search" name="search" placeholder="Mark..." value="{{search}}">
        <input type="text" name="from" class="difficulty" placeholder="Model..." value="{{model}}">
        <span>-</span>
        <input type="text" name="to" class="difficulty" placeholder="Gen..." value="{{gen}}">
        <input type="submit" value="search">
    </form>

    {{#each cars}}
        {{> car}}
    {{/each}}
</main>